apiVersion: v1
kind: Service
metadata:
  name: {{ include "kindl.fullname" . }}
  labels:
    {{- include "kindl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kindl.selectorLabels" . | nindent 4 }}
