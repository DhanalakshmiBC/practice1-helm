apiVersion: v1
kind: Service
metadata:
  name: {{ include "appforgit.fullname" . }}
  labels:
    {{- include "appforgit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appforgit.selectorLabels" . | nindent 4 }}
